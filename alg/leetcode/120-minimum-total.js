/**
 * 例如[[2], [3,4], [6,5,7], [4,1,8,3]]
 *     [2]
 *    [3,4]
 *   [6,5,7]
 *  [4,1,8,3]
 * 计算从上到下最短距离，且只能去相邻
 * @param {array} arr 
 */
function miniMumTotal(arr) {
  var l = arr.length,
    ll = arr[arr.length -1].length

  // 从下往上累加，计算最终最小值
  
}
